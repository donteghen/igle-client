// import {connect} from 'react-redux'

export default function UserRequests () {
    return (
        <div>User Requests</div>
    )
}
// const mapStateToProps = ()
// export default connect()(UserRequests)