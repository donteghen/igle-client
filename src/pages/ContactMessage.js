// import {connect} from 'react-redux'

export default function ContactMessages () {
    return (
        <div>Contact Messages</div>
    )
}
// const mapStateToProps = ()
// export default connect()(ContactMessages)